#include <iostream>   //Maaz Ali Nadeem , 20i-0452, AI(J)
#include <iomanip>
#include <string>
using namespace std;
int main()
{
	string new_install, condition_exist, option_add;
	cout<<setw(55)<<"HP Diagnosis Program"<<endl<<endl<<endl;
	cout<<"Recording symptom information - DONE"<<endl;
	cout<<"Rebooting server to check if condition exists - DONE"<<endl<<endl;
	
	cout<<"Is this a newly installed server? ";
	cin>>new_install;
	
	if ( new_install == "yes")
	{
		cout<<"Resetting components that may have come loose during shipping - DONE"<<endl;
		cout<<"Does condition still exist? ";
		cin>> condition_exist;
		if (condition_exist == "yes")
		{
			cout<<"Were configurations added or changed recently? ";
			cin>>option_add;
			if (option_add == "yes")
			{
				cout<<"Isolating changes, verifying installation, restoring server to last known state - DONE"<<endl;
				cout<<"Does condition still exist? ";
				cin>> condition_exist;
				if (condition_exist == "yes")
				{
					cout<<"isolating and minimizing the memory configuration"<<endl;
					cout<<"Does condition still exist? ";
					cin>> condition_exist;
					if (condition_exist == "yes")
					{
						cout<<"Breaking server down to minimal configuration - DONE";
						cout<<"Does condition still exist? ";
						cin>> condition_exist;
						if (condition_exist == "yes")
						{
							cout<<"Troubleshooting or replacing basic server parts - DONE"<<endl;
							cout<<"Does condition still exist? ";
							cin>> condition_exist;
							if (condition_exist == "yes")
							{
								cout<<"Ensure following information is available"<<endl;
								cout<<" 1. Survey configuration snapchosts"<<endl;
								cout<<" 2. OS Event log file"<<endl;
								cout<<" 3. Full crash dump"<<endl;
							}
							else
							{
								cout<<"Recording symptom and error information on repair tag if sending back a failed part- DONE"<<endl;
							}
						}
						else
						{
							cout<<"Adding one part at a time back to configuration to isolate faulty component - DONE"<<endl;
							cout<<"Does condition still exist? ";
							cin>> condition_exist;
							if (condition_exist =="yes")
							{
								cout<<"Ensure following information is available"<<endl;
								cout<<" 1. Survey configuration snapchosts"<<endl;
								cout<<" 2. OS Event log file"<<endl;
								cout<<" 3. Full crash dump"<<endl;
							}
							else
							{
								cout<<"Recording action taken - DONE";
							}
						}
					}
					else
					{
						cout<<"Recording action taken - DONE";
					}
					 
				}
				else
				{
					cout<<"Recording action taken - DONE";
				}
			}
			else
			{
				cout<<"Checking service notifications - DONE"<<endl;
				cout<<"Downloading latest software from HP website - DONE"<<endl;
				cout<<"Does condition still exist? ";
				cin>> condition_exist;
				if (condition_exist == "yes")
				{
					cout<<"isolating and minimizing the memory configuration"<<endl;
					cout<<"Does condition still exist? ";
					cin>> condition_exist;
					if (condition_exist == "yes")
					{
						cout<<"Breaking server down to minimal configuration - DONE";
						cout<<"Does condition still exist? ";
						cin>> condition_exist;
						if (condition_exist == "yes")
						{
							cout<<"Troubleshooting or replacing basic server parts - DONE"<<endl;
							cout<<"Does condition still exist? ";
							cin>> condition_exist;
							if (condition_exist == "yes")
							{
								cout<<"Ensure following information is available"<<endl;
								cout<<" 1. Survey configuration snapchosts"<<endl;
								cout<<" 2. OS Event log file"<<endl;
								cout<<" 3. Full crash dump"<<endl;
							}
							else
							{
								cout<<"Recording symptom and error information on repair tag if sending back a failed part- DONE"<<endl;
							}
						}
						else
						{
							cout<<"Adding one part at a time back to configuration to isolate faulty component - DONE"<<endl;
							cout<<"Does condition still exist? ";
							cin>> condition_exist;
							if (condition_exist =="yes")
							{
								cout<<"Ensure following information is available"<<endl;
								cout<<" 1. Survey configuration snapchosts"<<endl;
								cout<<" 2. OS Event log file"<<endl;
								cout<<" 3. Full crash dump"<<endl;
							}
							else
							{
								cout<<"Recording action taken - DONE";
							}
						}
					}
					else
					{
						cout<<"Recording action taken - DONE";
					}
					 
				}
				else
				{
					cout<<"Recording action taken - DONE";
				}
				
				
			}
		}
		else
		{
			cout<<"Recording action taken - DONE";
		}
	}
	else
	{
		cout<<"Were configurations added or changed recently? ";
		cin>>option_add;
		cout<<"Were configurations added or changed recently? ";
			cin>>option_add;
			if (option_add == "yes")
			{
				cout<<"Isolating changes, verifying installation, restoring server to last known state - DONE"<<endl;
				cout<<"Does condition still exist? ";
				cin>> condition_exist;
				if (condition_exist == "yes")
				{
					cout<<"isolating and minimizing the memory configuration"<<endl;
					cout<<"Does condition still exist? ";
					cin>> condition_exist;
					if (condition_exist == "yes")
					{
						cout<<"Breaking server down to minimal configuration - DONE";
						cout<<"Does condition still exist? ";
						cin>> condition_exist;
						if (condition_exist == "yes")
						{
							cout<<"Troubleshooting or replacing basic server parts - DONE"<<endl;
							cout<<"Does condition still exist? ";
							cin>> condition_exist;
							if (condition_exist == "yes")
							{
								cout<<"Ensure following information is available"<<endl;
								cout<<" 1. Survey configuration snapchosts"<<endl;
								cout<<" 2. OS Event log file"<<endl;
								cout<<" 3. Full crash dump"<<endl;
							}
							else
							{
								cout<<"Recording symptom and error information on repair tag if sending back a failed part- DONE"<<endl;
							}
						}
						else
						{
							cout<<"Adding one part at a time back to configuration to isolate faulty component - DONE"<<endl;
							cout<<"Does condition still exist? ";
							cin>> condition_exist;
							if (condition_exist =="yes")
							{
								cout<<"Ensure following information is available"<<endl;
								cout<<" 1. Survey configuration snapchosts"<<endl;
								cout<<" 2. OS Event log file"<<endl;
								cout<<" 3. Full crash dump"<<endl;
							}
							else
							{
								cout<<"Recording action taken - DONE";
							}
						}
					}
					else
					{
						cout<<"Recording action taken - DONE";
					}
					 
				}
				else
				{
					cout<<"Recording action taken - DONE";
				}
			}
			else
			{
				cout<<"Checking service notifications - DONE"<<endl;
				cout<<"Downloading latest software from HP website - DONE"<<endl;
				cout<<"Does condition still exist? ";
				cin>> condition_exist;
				if (condition_exist == "yes")
				{
					cout<<"isolating and minimizing the memory configuration"<<endl;
					cout<<"Does condition still exist? ";
					cin>> condition_exist;
					if (condition_exist == "yes")
					{
						cout<<"Breaking server down to minimal configuration - DONE";
						cout<<"Does condition still exist? ";
						cin>> condition_exist;
						if (condition_exist == "yes")
						{
							cout<<"Troubleshooting or replacing basic server parts - DONE"<<endl;
							cout<<"Does condition still exist? ";
							cin>> condition_exist;
							if (condition_exist == "yes")
							{
								cout<<"Ensure following information is available"<<endl;
								cout<<" 1. Survey configuration snapchosts"<<endl;
								cout<<" 2. OS Event log file"<<endl;
								cout<<" 3. Full crash dump"<<endl;
							}
							else
							{
								cout<<"Recording symptom and error information on repair tag if sending back a failed part- DONE"<<endl;
							}
						}
						else
						{
							cout<<"Adding one part at a time back to configuration to isolate faulty component - DONE"<<endl;
							cout<<"Does condition still exist? ";
							cin>> condition_exist;
							if (condition_exist =="yes")
							{
								cout<<"Ensure following information is available"<<endl;
								cout<<" 1. Survey configuration snapchosts"<<endl;
								cout<<" 2. OS Event log file"<<endl;
								cout<<" 3. Full crash dump"<<endl;
							}
							else
							{
								cout<<"Recording action taken - DONE";
							}
						}
					}
					else
					{
						cout<<"Recording action taken - DONE";
					}
					 
				}
				else
				{
					cout<<"Recording action taken - DONE";
				}
		
	}
}}
